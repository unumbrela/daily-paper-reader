---
title: Physics-informed data-driven inference of an interpretable equivariant LES model of incompressible fluid turbulence
title_zh: 物理信息告知的数据驱动推断：一种用于不可压缩流体湍流的可解释等变 LES 模型
authors: "Matteo Ugliotti, Brandon Choi, Mateo Reynoso, Daniel R. Gurevich, Roman O. Grigoriev"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15743v1"
tags: ["keyword:SR", "query:SR"]
score: 9.0
evidence: 用于流体湍流物理定律提取的符号数据驱动模型
tldr: 本研究针对传统大涡模拟（LES）中亚格子尺度模型受限于现象学假设、难以准确描述能量通量的问题，提出了一种物理告知的数据驱动符号模型。该模型无需预设参数，通过引入一个二阶张量场来描述亚格子尺度特征，其结构兼具LES的空间粗粒化与RANS的场描述优势。在多种二维湍流测试中，该模型在预测局部能量和涡方通量方面优于现有主流模型，展现了极高的准确性和可解释性。
motivation: 传统的现象学假设限制了亚格子尺度模型在复杂相干结构下准确描述能量和涡方局部通量的能力。
method: 采用物理告知的数据驱动方法，推导出一种无需调节参数、包含额外二阶张量场的符号等变亚格子模型。
result: 在先验和后验基准测试中，该模型在多种二维湍流流动下的预测性能均优于领先的LES模型。
conclusion: 引入二阶张量场对于准确表征亚格子应力张量及各类物理通量至关重要，为构建可解释的湍流模型提供了新路径。
---

## 摘要
限制性的现象学假设是开发精确流体湍流亚网格尺度模型的主要障碍。具体而言，在存在多种相干结构的情况下，这些假设限制了模型描述关键关注量（如能量和拟能的局部通量）的能力。本文介绍了一种符号化数据驱动的亚网格尺度模型，该模型无需现象学假设且没有可调参数，但其性能优于领先的 LES 模型。先验和后验基准测试的结合表明，该模型在广泛的二维湍流中能够对包括局部通量在内的各种物理量产生准确预测。虽然该模型是使用 LES 风格的空间粗粒化推断而来的，但其结构更接近 RANS 模型，因为它采用了一个额外的场来描述亚网格尺度。我们发现，该场必须具有二阶张量结构，才能正确表示亚网格尺度应力张量的分量以及各种通量。

## Abstract
Restrictive phenomenological assumptions represent a major roadblock for the development of accurate subgrid-scale models of fluid turbulence. Specifically, these assumptions limit a model's ability to describe key quantities of interest, such as local fluxes of energy and enstrophy, in the presence of diverse coherent structures. This paper introduces a symbolic data-driven subgrid-scale model that requires no phenomenological assumptions and has no adjustable parameters, yet it outperforms leading LES models. A combination of a priori and a posteriori benchmarks shows that the model produces accurate predictions of various quantities including local fluxes across a broad range of two-dimensional turbulent flows. While the model is inferred using LES-style spatial coarse-graining, its structure is more similar to RANS models, as it employs an additional field to describe subgrid scales. We find that this field must have a rank-two tensor structure in order to correctly represent both the components of the subgrid-scale stress tensor and the various fluxes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合物理信息与数据驱动技术的亚格子尺度（SGS）模型，旨在解决传统大涡模拟（LES）在处理复杂湍流结构时的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统 LES 模型（如 Smagorinsky 模型）严重依赖于现象学假设（如 Boussinesq 假设、各向同性、尺度不变性）。这些假设在存在强相干结构（Coherent Structures）的流动中往往失效，导致模型无法准确描述能量和涡方的局部通量，尤其是难以捕捉“能量反向散射”（Backscatter，即能量从细微尺度流向大尺度的现象）。
*   **研究背景**：虽然机器学习（ML）已被用于开发数据驱动的闭合模型，但现有方法往往缺乏可解释性、不满足物理对称性（如旋转等变性），且在长期演化中容易出现数值不稳定。

### 2. 方法论：NGMR 模型
*   **核心思想**：利用 **SPIDER**（稀疏物理告知经验关系发现）框架，从高保真直接数值模拟（DNS）数据中自动推断出具有物理对称性的符号方程。
*   **关键技术细节**：
    *   **变量引入**：研究发现仅靠解析尺度变量（如过滤后的速度 $\bar{u}$）不足以描述亚格子效应。模型引入了一个额外的**二阶对称无迹张量场 $R_{ij}$**（类似于 RANS 模型中的雷诺应力张量）来描述亚格子尺度特征。
    *   **模型结构（NGMR）**：
        1.  **应力张量参数化**：将亚格子应力 $\tau_{ij}$ 分解为非线性梯度项（NGM4，基于泰勒展开）和新引入的张量场 $R_{ij}$。
        2.  **演化方程**：为 $R_{ij}$ 推导了一个动力学演化方程，包含平流项、生产项、扩散项和耗散项。
    *   **物理约束**：通过群表示理论强制模型满足**伽利略不变性**和**旋转等变性**。
    *   **数值稳定性**：引入了基于量纲分析的超粘性正则化项，以防止伪能量在离散网格的高波数段堆积。

### 3. 实验设计
*   **数据集与场景**：
    *   **二维不可压缩湍流**：包括受迫统计平稳湍流（具有直接和反向级联）、瞬态自由衰减湍流。
    *   **雷诺数范围**：$Re$ 高达 $3 \times 10^6$。
    *   **训练数据**：使用过滤后的 DNS（FDNS）数据，网格分辨率从 $2048^2$ 到 $4096^2$。
*   **Benchmark（基准）**：以 FDNS 作为真值。
*   **对比方法**：
    *   动态 Smagorinsky 模型（Dynamic Smagorinsky）。
    *   相似性模型（Similarity Model）。
    *   动态混合模型（Dynamic Mixed Model）。
    *   四阶非线性梯度模型（NGM4）。

### 4. 资源与算力
*   **软件框架**：使用 JAX-CFD 框架进行模拟，采用伪谱法求解器。
*   **算力说明**：文中提到使用了佐治亚理工学院的 PACE 高性能计算资源。
*   **具体细节**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长，但提到 DNS 模拟在高达 $8192^2$ 的网格上运行，而 LES 模拟在 $128^2$ 到 $1024^2$ 的网格上运行。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 5 种不同的流动配置（F1-F5），涉及多种粘度系数（$10^{-4}$ 到 $10^{-7}$）和不同的过滤尺度 $\Delta$。
*   **充分性评价**：实验设计非常充分。作者不仅进行了**先验（A priori）**测试（评估模型对瞬时应力的预测准确度），还进行了长时间步的**后验（A posteriori）**在线模拟（评估模型的稳定性、能量谱分布、涡度概率密度函数 PDF 等）。
*   **客观性**：通过在训练集之外的流动（如带有瑞利摩擦的受迫流动）上进行测试，验证了模型的泛化能力。

### 6. 主要结论与发现
*   **性能卓越**：NGMR 模型在预测局部能量通量和亚格子应力方面几乎完美匹配 DNS 数据，显著优于所有传统的现象学模型。
*   **捕捉反向散射**：该模型是唯一能准确预测二维湍流中能量反向散射分布的模型。
*   **结构必要性**：研究证明，要同时准确预测应力分量和能量通量，必须引入二阶张量场，单一的标量场（如常见的 RANS 闭合）是不够的。
*   **稳定性**：配合正则化项，模型在极高雷诺数下依然保持数值稳定，且短期预测精度远超同类模型。

### 7. 优点
*   **可解释性**：模型以显式符号方程形式给出，而非黑盒神经网络。
*   **无参数调节**：模型系数通过数据推断确定，无需针对不同流动手动调参。
*   **物理一致性**：严格遵守对称性要求，保证了在不同坐标系下的物理一致性。
*   **兼顾 LES 与 RANS 优势**：结合了 LES 的空间解析能力和 RANS 的场描述结构。

### 8. 不足与局限
*   **维度限制**：目前仅在**二维**流动中进行了验证。虽然理论可扩展至三维，但三维湍流的物理机制（如涡旋拉伸）更为复杂，计算成本也会显著增加。
*   **正则化依赖**：模型仍需依赖人工设计的超粘性项来维持伪谱法的数值稳定性，这在一定程度上引入了经验性。
*   **计算开销**：由于增加了一个张量演化方程，其计算成本略高于最简单的梯度模型（约为 NGM4 的 2 倍），但在高精度要求下这种开销是合理的。

（完）
