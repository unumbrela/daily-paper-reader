---
title: "Physics-Informed Machine Learning for Vessel Shaft Power and Fuel Consumption Prediction: Interpretable KAN-based Approach"
title_zh: 物理信息机器学习用于船舶轴功率和燃油消耗预测：一种基于可解释 KAN 的方法
authors: "Hamza Haruna Mohammed, Dusica Marijan, Arnbjørn Maressa"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22055v1"
tags: ["query:sr"]
score: 8.0
evidence: 物理信息KAN用于航运数据的科学发现和物理规律提取
tldr: 本研究提出物理信息驱动的 Kolmogorov-Arnold 网络（PI-KAN），用于精确预测船舶轴功率与油耗。该模型通过集成物理约束损失函数与可解释的 KAN 架构，解决了传统模型在复杂环境下精度与物理一致性难以兼顾的问题。实验证明，PI-KAN 在多艘货轮数据上的表现优于现有基准，且能自动发现符合领域知识的物理规律，为船舶能效监控提供了稳健且透明的决策支持。
motivation: 旨在解决传统物理模型难以应对现实环境多变性，以及纯数据驱动模型在船舶性能预测中缺乏物理可解释性的挑战。
method: 提出 PI-KAN 模型，将物理信息损失函数与具有单变量特征转换能力的 Kolmogorov-Arnold 网络相结合，并采用无泄漏链式预测流程。
result: 在五艘货轮的实测数据中，该模型在轴功率和油耗预测的各项精度指标上均优于传统多项式方法和神经网络基准。
conclusion: PI-KAN 成功实现了预测精度与物理可解释性的平衡，证明了其作为船舶性能监测和运营决策支持工具的有效性。
---

## 摘要
准确预测轴转速、轴功率和燃油消耗对于提高海上运输的运营效率和可持续性至关重要。传统的基于物理的模型具有可解释性，但在应对现实世界的变异性方面表现不佳，而纯数据驱动的方法虽然实现了高精度，却以牺牲物理合理性为代价。本文介绍了一种物理信息 Kolmogorov-Arnold 网络（PI-KAN），这是一种将可解释的单变量特征变换与物理信息损失函数以及无泄漏链式预测流水线相结合的混合方法。利用来自五艘货船的运营和环境数据，PI-KAN 的表现始终优于传统的多项式方法和神经网络基准模型。该模型在所有船舶的轴功率和燃油消耗预测中均实现了最低的平均绝对误差（MAE）和均方根误差（RMSE），以及最高的决定系数（R^2），同时保持了物理一致性。可解释性分析揭示了对领域一致性依赖关系的重新发现，例如类三次方的速度-功率关系以及类余弦的海浪和风力影响。这些结果表明，PI-KAN 兼具预测准确性和可解释性，为运营环境下的船舶性能监测和决策支持提供了一个稳健的工具。

## Abstract
Accurate prediction of shaft rotational speed, shaft power, and fuel consumption is crucial for enhancing operational efficiency and sustainability in maritime transportation. Conventional physics-based models provide interpretability but struggle with real-world variability, while purely data-driven approaches achieve accuracy at the expense of physical plausibility. This paper introduces a Physics-Informed Kolmogorov-Arnold Network (PI-KAN), a hybrid method that integrates interpretable univariate feature transformations with a physics-informed loss function and a leakage-free chained prediction pipeline. Using operational and environmental data from five cargo vessels, PI-KAN consistently outperforms the traditional polynomial method and neural network baselines. The model achieves the lowest mean absolute error (MAE) and root mean squared error (RMSE), and the highest coefficient of determination (R^2) for shaft power and fuel consumption across all vessels, while maintaining physically consistent behavior. Interpretability analysis reveals rediscovery of domain-consistent dependencies, such as cubic-like speed-power relationships and cosine-like wave and wind effects. These results demonstrate that PI-KAN achieves both predictive accuracy and interpretability, offering a robust tool for vessel performance monitoring and decision support in operational settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Physics-Informed Machine Learning for Vessel Shaft Power and Fuel Consumption Prediction: Interpretable KAN-based Approach》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证船舶性能预测（轴转速 RPM、轴功率、燃油消耗）高精度的同时，确保模型符合物理规律且具备可解释性。
*   **研究背景**：
    *   **传统物理模型**：基于螺旋桨理论和热力学，虽具有解释性，但难以捕捉复杂的现实环境变异性（如船体污损、极端天气）。
    *   **纯数据驱动模型**：如神经网络，虽精度高，但属于“黑盒”，且常给出违反物理常识的预测（如速度增加油耗反而降低）。
    *   **现有混合方法**：在平衡物理约束与经验数据方面存在困难，且缺乏对航海工程师透明的解释机制。

### 2. 方法论
论文提出了 **PI-KAN（物理信息 Kolmogorov-Arnold 网络）**，核心思想包括：
*   **KAN 架构基础**：不同于传统 MLP 在节点上使用固定激活函数，KAN 在边上学习单变量函数（Univariate Functions）。它将多元函数分解为单变量变换的线性组合，天然具备可解释性。
*   **物理信息损失函数**：
    *   **总损失**：$L = L_{data} + \lambda L_{physics} + L_{reg}$。
    *   **物理约束**：引入经验阻力模型（静水、风、浪阻力）计算理论功率，并利用螺旋桨“立方定律”（$P \propto n^3$）和热效率关系约束功率与油耗。
    *   **自动平衡机制**：通过参数 $\lambda$ 动态调整物理损失权重，防止物理约束过强导致欠拟合或过弱导致物理失真。
*   **无泄漏链式预测流程**：采用“RPM $\to$ 轴功率 $\to$ 燃油消耗”的顺序预测。利用 **Out-of-Fold (OOF) 堆叠技术**，确保下游模型在训练时使用的是上游模型的预测值而非真实值，模拟真实部署环境，防止信息泄漏。

### 3. 实验设计
*   **数据集**：来自 5 艘货轮（A-6, A-8, A-10, A-12, A-13）的 15 分钟粒度运营和环境日志，涵盖超过 5 年的数据，包含传感器数据和 Copernicus 海洋服务提供的气象数据。
*   **对比基准（Benchmarks）**：
    1.  **多项式模型 (Polynomial Model)**：传统的轻量级基准。
    2.  **多层感知机 (MLP)**：纯数据驱动的深度学习基准。
    3.  **标准 KAN**：未加入物理信息的 KAN 模型。
*   **评估指标**：MAE（平均绝对误差）、RMSE（均方根误差）、$R^2$（决定系数）以及针对极端天气的 MAPE。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到使用了 Adam 优化器，学习率为 $10^{-3}$，Batch Size 为 32，训练轮数上限为 150 轮并带有早停机制。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 艘不同规模、不同维护状态（干船坞日期不同）的船舶进行了独立验证。
*   **充分性**：
    *   **多维度验证**：不仅对比了整体精度，还针对“对抗性天气”（极端海况）进行了专门的案例研究。
    *   **可解释性分析**：通过可视化 KAN 学习到的单变量样条函数，验证了模型是否捕捉到了物理规律（如速度-功率的立方关系）。
    *   **客观性**：使用了长达数年的真实运营数据，而非模拟数据，实验设计较为客观公平。

### 6. 主要结论与发现
*   **精度领先**：PI-KAN 在所有船舶的**轴功率**和**燃油消耗**预测中均取得了最低的误差（MAE/RMSE）和最高的 $R^2$。
*   **物理一致性**：在 RPM 预测上，多项式模型在某些船舶上略优，但在涉及能量转换的复杂目标（功率、油耗）上，PI-KAN 的物理引导优势显著。
*   **规律再发现**：PI-KAN 自动学习到了符合领域知识的曲线，如类三次方的速度-功率关系、类余弦的风/浪方向影响，以及吃水深度对功率的非线性影响。
*   **极端天气表现**：在恶劣海况下，PI-KAN 表现出更强的鲁棒性，能够反映出船舶减速避浪等操作策略对能耗的影响。

### 7. 优点与亮点
*   **架构创新**：首次将 KAN 引入船舶性能建模，利用其单变量变换特性解决了深度学习的“黑盒”问题。
*   **自适应平衡**：自动调整物理损失权重的机制解决了传统 PINN（物理信息神经网络）中物理项与数据项难以平衡的痛点。
*   **工业实用性**：链式预测和 OOF 堆叠设计非常贴合实际航运监测系统的部署逻辑。

### 8. 不足与局限
*   **高阶交互限制**：KAN 的加性结构虽然提升了解释性，但可能难以捕捉极高阶的特征交互（如吃水、纵倾与海况之间的复杂耦合）。
*   **计算成本**：相比于简单的多项式回归，KAN 的训练和推理成本更高，可能限制其在算力受限的船端设备上的实时部署。
*   **稳态假设**：物理约束基于准稳态假设，对于瞬态过程（如剧烈操舵或急停）的捕捉能力有限。
*   **超参数敏感性**：尽管有自动平衡机制，但物理约束的引入仍增加了模型调优的复杂度。

（完）
