---
title: "FedKRSO: Communication and Memory Efficient Federated Fine-Tuning of Large Language Models"
title_zh: FedKRSO：通信与内存高效的大语言模型联邦微调
authors: "Guohao Yang, Tongle Wu, Yuanxiong Guo, Ying Sun, Yanmin Gong"
date: 2026-02-03
pdf: "https://arxiv.org/pdf/2602.03019v1"
tags: ["query:sr-llm"]
score: 10.0
evidence: 大语言模型的联邦微调与参数高效优化
tldr: 针对大语言模型在联邦学习中微调时面临的高通信和内存开销问题，本文提出FedKRSO框架。该方法通过在共享的随机低维子空间内进行模型更新，并仅传输更新累加器，实现了在资源受限的边缘设备上进行高效的全参数微调。实验证明，FedKRSO在显著降低开销的同时，性能接近全参数微调，优于传统的参数高效微调（PEFT）方法，为边缘侧联邦微调提供了新思路。
motivation: 现有的联邦学习微调方法在降低通信和内存成本时往往会牺牲模型性能，难以在资源受限的边缘设备上实现高效且高性能的LLM适配。
method: 提出FedKRSO方法，利用服务器生成的共享随机子空间进行本地更新，并仅上传子空间内的更新累加器以实现高效聚合。
result: 在GLUE基准测试上的实验表明，FedKRSO在保持极低通信和内存开销的同时，性能显著优于PEFT并接近全参数微调。
conclusion: FedKRSO为资源受限环境下的联邦大模型微调提供了一种兼顾效率与性能的新方案，并具有严谨的收敛性理论保证。
---

## 摘要
微调是将通用大语言模型（LLMs）适配到特定领域任务的关键。联邦学习（FL）作为一种利用去中心化数据进行协作模型训练的隐私保护框架，在 LLM 微调中日益受到关注，但由于在资源受限的客户端上传输全量模型参数和计算全量梯度的成本极高，其应用仍面临挑战。虽然参数高效微调（PEFT）方法在 FL 中被广泛用于降低通信和内存成本，但与全参数微调（FFT）相比，它们往往会牺牲模型性能。本文提出了 FedKRSO（联邦 K-种子随机子空间优化），这是一种在联邦设置下实现 LLM 通信与内存高效 FFT 的新方法。在 FedKRSO 中，客户端在由服务器生成的共享随机低维子空间集合内更新模型，以节省内存消耗。此外，客户端在每个 FL 轮次中不再传输全量模型参数，而是仅向服务器发送沿子空间的模型更新累加器，从而实现高效的全局模型聚合与分发。通过采用这些策略，FedKRSO 能够大幅降低通信和内存开销，同时克服 PEFT 的性能局限，使其性能接近联邦 FFT。本文在通用 FL 设置下对 FedKRSO 的收敛特性进行了严格分析。在多种 FL 场景下针对 GLUE 基准进行的广泛实验表明，FedKRSO 既实现了卓越的性能，又保持了较低的通信和内存开销，为在资源受限的边缘端进行联邦 LLM 微调铺平了道路。

## Abstract
Fine-tuning is essential to adapt general-purpose large language models (LLMs) to domain-specific tasks. As a privacy-preserving framework to leverage decentralized data for collaborative model training, Federated Learning (FL) is gaining popularity in LLM fine-tuning, but remains challenging due to the high cost of transmitting full model parameters and computing full gradients on resource-constrained clients. While Parameter-Efficient Fine-Tuning (PEFT) methods are widely used in FL to reduce communication and memory costs, they often sacrifice model performance compared to FFT. This paper proposes FedKRSO (Federated $K$-Seed Random Subspace Optimization), a novel method that enables communication and memory efficient FFT of LLMs in federated settings. In FedKRSO, clients update the model within a shared set of random low-dimension subspaces generated by the server to save memory usage. Furthermore, instead of transmitting full model parameters in each FL round, clients send only the model update accumulators along the subspaces to the server, enabling efficient global model aggregation and dissemination. By using these strategies, FedKRSO can substantially reduce communication and memory overhead while overcoming the performance limitations of PEFT, closely approximating the performance of federated FFT. The convergence properties of FedKRSO are analyzed rigorously under general FL settings. Extensive experiments on the GLUE benchmark across diverse FL scenarios demonstrate that FedKRSO achieves both superior performance and low communication and memory overhead, paving the way towards on federated LLM fine-tuning at the resource-constrained edge.

---

## 论文详细总结（自动生成）

### 论文总结：FedKRSO —— 通信与内存高效的大语言模型联邦微调

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的普及，针对特定领域任务的微调变得至关重要。然而，在**联邦学习（FL）**场景下微调 LLM 面临严峻挑战：
*   **全参数微调（FFT）的局限性**：在资源受限的边缘设备上，传输数以十亿计的参数（通信开销）和存储全量梯度及优化器状态（内存开销）是不切实际的。
*   **参数高效微调（PEFT，如 LoRA）的不足**：虽然降低了开销，但在数据异构（Non-IID）环境下，LoRA 的低秩更新往往无法捕捉全局模型的动态，导致性能显著低于 FFT。
*   **研究目标**：开发一种既能保持 PEFT 级别的资源效率，又能逼近 FFT 性能的联邦微调方法。

#### 2. 论文提出的方法论：FedKRSO
FedKRSO（Federated K-Seed Random Subspace Optimization）的核心思想是**在共享的随机低维子空间内进行模型优化**。
*   **核心思想**：服务器维护一组随机种子（Seeds）和对应的模型更新累加器。客户端利用这些种子生成投影矩阵，将高维梯度投影到低维子空间进行更新。
*   **关键技术细节**：
    *   **内存高效的本地训练**：客户端不直接计算全量梯度，而是通过链式法则 $\nabla_B F_n(W + BP) = \nabla F_n(W + BP)P^\top$ 直接计算子空间内的压缩梯度。这避免了存储全量梯度的峰值内存开销。
    *   **K-种子机制与模型重构**：服务器分发 $K$ 个随机种子，客户端根据种子生成相同的随机矩阵 $P$。客户端在本地训练后，仅上传子空间内的更新累加器 $B$。服务器聚合这些累加器，客户端再结合种子重构最新的全局模型。
    *   **动量加速**：在子空间内应用类似 Adam 的优化器，但仅存储低维的动量状态，进一步节省内存。
*   **算法流程**：服务器广播种子和累加器 $\rightarrow$ 客户端重构模型 $\rightarrow$ 客户端在随机子空间内进行多轮本地迭代 $\rightarrow$ 客户端上传更新后的累加器 $\rightarrow$ 服务器进行种子对种子的聚合。

#### 3. 实验设计
*   **数据集**：使用 NLP 权威基准 **GLUE**，包含 CoLA、SST-2、MRPC、QQP、STS-B、MNLI、QNLI 和 RTE 八项任务。
*   **实验场景**：
    *   **IID（独立同分布）**：数据均匀分布。
    *   **Non-IID（非独立同分布）**：使用 Dirichlet 分布（$\alpha=0.5, 0.25$）模拟不同程度的数据异构性。
*   **Benchmark（基准模型）**：RoBERTa-base (125M) 和 RoBERTa-large (350M)。
*   **对比方法**：
    *   **FedFFT**：联邦全参数微调（性能上限）。
    *   **FedIT**：基于 LoRA 的联邦指令微调。
    *   **FFA-LoRA**：冻结部分 LoRA 权重的最先进 PEFT 变体。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备了 **4 张 NVIDIA Quadro RTX 8000 GPU**（每张 48GB 显存）的 Linux 服务器上运行。
*   **软件框架**：基于 PyTorch 实现。
*   **训练时长**：文中未明确给出总训练时长，但提到每个任务运行 30 个联邦轮次，每个轮次包含 100 次本地迭代。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个数据集、2 种模型规模、3 种数据分布设置（IID, $\alpha=0.5, 0.25$），每组实验均使用 3 个随机种子取平均值。
*   **消融实验**：针对本地训练间隔 $J$ 和种子数量 $K$ 进行了专门的消融研究，验证了超参数的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅对比了模型准确率，还详细测量了**峰值 GPU 内存占用**和**每轮通信开销**，能够客观、公平地反映该方法在资源受限环境下的优势。

#### 6. 论文的主要结论与发现
*   **性能优越性**：FedKRSO 在所有任务和异构设置下均优于 LoRA 及其变体。在高度异构（$\alpha=0.25$）的情况下，FedKRSO 的表现比 FedIT 高出约 1.17% 至 1.69%，非常接近 FedFFT。
*   **资源高效性**：
    *   **内存**：FedKRSO 的峰值内存占用甚至低于 LoRA（例如 RoBERTa-large 下，FedKRSO 为 5.00GB，LoRA 为 5.62GB），因为它优化了梯度计算过程。
    *   **通信**：通信开销比 FedFFT 降低了两个数量级（约 300 倍），虽然略高于 LoRA，但在可接受范围内。
*   **理论保证**：论文通过严谨的数学推导，证明了该算法在非凸损失函数下的收敛性。

#### 7. 优点
*   **内存管理创新**：通过直接计算压缩梯度而非“先计算全量再压缩”，解决了边缘设备处理大模型时的 OOM（内存溢出）痛点。
*   **性能与效率的平衡**：成功打破了“PEFT 性能必输 FFT”的固有印象，在联邦场景下提供了极佳的折中方案。
*   **理论与实践结合**：既有严格的收敛性证明，又有在标准基准上的显著实验提升。

#### 8. 不足与局限
*   **模型规模限制**：实验主要集中在亿级参数模型（RoBERTa），尚未在百亿级（如 Llama-7B/70B）模型上进行大规模验证。
*   **通信开销略增**：相比于极度精简的 LoRA，FedKRSO 由于需要传输 $K$ 个累加器，通信量略有增加（尽管仍远小于 FFT）。
*   **超参数敏感性**：种子数量 $K$ 的选择需要在性能和通信成本之间进行权衡，对于不同任务可能需要微调。
*   **应用范围**：目前仅验证了 NLP 任务，在多模态或纯生成式任务（如长文本生成）上的表现尚待观察。

（完）
